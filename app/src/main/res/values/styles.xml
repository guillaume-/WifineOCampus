<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <!--<item TableName="colorPrimary">@android:color/transparent</item>-->
        <!--<item TableName="colorPrimaryDark">@android:color/transparent</item>-->
        <item name="colorAccent">@color/colorAccent</item>

    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar">
        <item name="elevation">0dp</item>
        <item name="colorPrimary">@color/colorNavigation</item>
        <item name="titleTextColor">@android:color/white</item>
        <item name="android:titleTextColor" tools:targetApi="m">@color/colorNavigation</item>
    </style>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TextSmallStyle" parent="android:TextAppearance.Small" >
        <item name="android:textSize">@dimen/text_size_small_material</item>
    </style>

</resources>
